/**
 * ${classInfo.className}Repository
 *
 * @author ${authorName}
 * @date ${.now?string('yyyy-MM-dd')}
 */
@Service
public class ${classInfo.className}Repository extends RepositoryServiceImpl<${classInfo.className}Mapper, ${classInfo.className}> {
}
